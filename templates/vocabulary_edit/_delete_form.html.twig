<form method="post" action="{{ path('app_vocabulary_edit_delete', {'id': vocabulary.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vocabulary.id) }}">
    <button class="btn">Delete</button>
</form>
