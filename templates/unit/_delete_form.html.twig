<form method="post" action="{{ path('app_unit_delete', {'id': unit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ unit.id) }}">
    <button class="btn">Delete</button>
</form>
